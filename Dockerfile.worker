FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# No EXPOSE — Render handles port mapping via $PORT env var automatically

CMD ["python", "job_worker_realtime.py"]
